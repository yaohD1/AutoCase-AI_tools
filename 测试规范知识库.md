# 测试规范知识库

## 通用规范

- P0：核心路径必须通过，否则阻塞上线
- P1：重要功能异常，影响主要用户体验
- P2：边界场景、UI细节、建议性问题
- 每个用例的预期结果必须可验证、明确
- 步骤描述要具体，避免"操作正常"等模糊描述

## 功能测试

- 正常流程覆盖：正向操作路径必须全部覆盖
- 异常分支：必填项为空提交、非法数据输入
- 权限校验：不同角色登录后的功能可见性
- 数据一致性：新增/编辑/删除后列表数据刷新正确
- 状态流转：各状态之间的切换是否合规
- 并发操作：同一数据多人同时编辑的冲突处理

## UI交互测试

- 各分辨率下的布局适配（重点关注移动端和PC端）
- 按钮点击态、禁用态、加载态的正确展示
- 弹窗/提示框的弹出和关闭是否流畅
- 页面跳转动画是否卡顿
- 深色模式/浅色模式的切换效果
- 键盘操作：Tab键焦点顺序、Enter键提交

## 边界值测试

- 输入框最小/最大字符数限制
- 数值类型的最大/最小值
- 空值、空格、特殊字符的输入处理
- 分页加载的首尾页、翻页边界
- 文件上传的大小上限、格式限制
- 时间段选择的起止时间逻辑

## 异常场景测试

- 网络断开/超时后的重试机制和提示
- 服务端返回错误码时的前端处理（500/403/404）
- 接口响应超时时的Loading状态和兜底展示
- Token过期后的自动跳转登录页
- 浏览器前进后退按钮的路由状态保持
- 弱网环境下的页面降级方案
